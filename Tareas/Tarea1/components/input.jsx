const Input = (props) => {  
    return <input 
    id={props.id} 
    type={props.type}
    name={props.id} 
    className={props.className} 
    placeholder={props.placeholder}></input>
}
export default Input;